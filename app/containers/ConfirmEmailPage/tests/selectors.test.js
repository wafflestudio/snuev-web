// import { fromJS } from 'immutable';
// import { makeSelectConfirmEmailPageDomain } from '../selectors';

// const selector = makeSelectConfirmEmailPageDomain();

describe('makeSelectConfirmEmailPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
