// import { fromJS } from 'immutable';
// import { makeSelectLectureDetailPageDomain } from '../selectors';

// const selector = makeSelectLectureDetailPageDomain();

describe('makeSelectLectureDetailPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
