// import { fromJS } from 'immutable';
// import { makeSelectEvaluatePageDomain } from '../selectors';

// const selector = makeSelectEvaluatePageDomain();

describe('makeSelectEvaluatePageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
