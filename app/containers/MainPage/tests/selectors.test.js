// import { fromJS } from 'immutable';
// import { makeSelectMainPageDomain } from '../selectors';

// const selector = makeSelectMainPageDomain();

describe('makeSelectMainPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
