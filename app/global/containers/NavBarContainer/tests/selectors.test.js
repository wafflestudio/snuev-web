// import { fromJS } from 'immutable';
// import { makeSelectNavBarContainerDomain } from '../selectors';

// const selector = makeSelectNavBarContainerDomain();

describe('makeSelectNavBarContainerDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
