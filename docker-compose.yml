version: '3'
services:
  app:
    build: .
    command: yarn start
    ports:
      - '3000:3000'
    volumes:
      - '.:/usr/src/app'
      - 'node_modules:/usr/src/app/node_modules'
    environment:
      - API_HOST=http://snuev-backend.herokuapp.com
    env_file: .env
volumes:
  node_modules:
